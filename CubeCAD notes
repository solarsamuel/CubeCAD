
linux install

sudo apt update
sudo apt install libsdl2-dev
Install additional development packages (optional but helpful):
bash
Copy code
sudo apt install libsdl2-image-dev libsdl2-ttf-dev libsdl2-mixer-dev
Verify Installation:

Check if SDL2 is installed:
bash
Copy code
sdl2-config --version

compile
g++ -o ccad ccad.cpp -lSDL2 -lSDL2_ttf

